# @praxislume/contracts

Shared Zod schemas and TypeScript types for PraxisLume.

The contracts package is the boundary between Flutter-facing API expectations, backend route validation, and tests. Runtime validation must happen at backend entrypoints.
